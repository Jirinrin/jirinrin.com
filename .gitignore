# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build
/dist

# large photoshop files
/ps

# raw Well of Memories originals (kept locally only - unoptimized, can be
# several MB each) and the small webp thumbnails `npm run thumbs`
# (scripts/gen-memories-thumbs.mjs) generates from them for the actual site
/src/assets/memories
/src/assets/memories-thumbs

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
